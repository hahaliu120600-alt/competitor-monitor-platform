# 竞品监测平台 - 快速启动指南

## ✅ 平台已搭建完成！

竞品监测平台已成功部署在你的Mac上，所有功能已就绪。

---

## 🚀 立即启动

### 方式1：使用启动脚本（推荐）

```bash
cd /Users/xiaoxiao.liu/竞品监测平台
./start.sh
```

### 方式2：手动启动

```bash
cd /Users/xiaoxiao.liu/竞品监测平台
source venv/bin/activate
python app.py
```

### 方式3：后台运行

```bash
cd /Users/xiaoxiao.liu/竞品监测平台
source venv/bin/activate
nohup python app.py > logs/app.log 2>&1 &
```

---

## 🌐 访问平台

启动后在浏览器中访问：

```
http://localhost:5000
```

或局域网访问（查看启动日志获取IP地址）：

```
http://你的IP:5000
```

---

## 📊 平台功能

### 1. Dashboard（首页）
- **KPI卡片**：监测竞品数、本周更新、公司动态、合作信息
- **最新动态**：时间线展示最近7天的所有更新
- **竞品快览**：快速查看所有竞品状态
- **快速操作**：一键触发数据采集、生成周报

### 2. 竞品列表
- 查看10个竞品的完整信息
- 按分类筛选（国际/国内/本品）
- 查看最新版本、更新频率、评分排名

### 3. 趋势分析
- 行业趋势图表（评分、排名、下载量）
- 更新频率对比
- 功能热点分析

### 4. 历史报告
- 查看所有生成的周报
- 在线预览和下载
- 手动生成新周报

### 5. 设置
- 查看系统配置
- 手动录入市场数据
- 系统信息查看

---

## ⏰ 自动任务

平台已配置以下自动任务：

| 任务 | 时间 | 说明 |
|------|------|------|
| 📱 每日数据采集 | 每天 02:00 | 自动采集10个竞品的更新、新闻、合作信息 |
| 📄 生成周报 | 每周五 17:00 | 自动生成Markdown格式周报并保存 |
| 💾 数据库备份 | 每周日 03:00 | 自动备份数据库，保留30天 |

> **注意**：自动任务仅在平台运行时生效

---

## 🔑 配置Claude API（重要）

要使用自动数据采集功能，需要配置Claude API Key：

```bash
# 临时设置（当前终端有效）
export ANTHROPIC_API_KEY="sk-ant-your-key-here"

# 永久设置（添加到 ~/.zshrc 或 ~/.bash_profile）
echo 'export ANTHROPIC_API_KEY="sk-ant-your-key-here"' >> ~/.zshrc
source ~/.zshrc
```

获取API Key：https://console.anthropic.com/

> 💡 **不配置API Key也可以运行**：平台会启动，但自动采集功能无法使用，需手动录入数据

---

## 📝 数据采集说明

### 自动采集（需API Key）
- **App更新**：每天采集所有竞品的版本更新信息
- **公司动态**：搜索融资、收购、战略、人事新闻
- **合作信息**：每周一、三、五采集（降低频率）
- **市场数据**：自动搜索公开数据（效果有限，建议手动补充）

### 手动补充
1. 访问"设置"页面
2. 找到"手动录入市场数据"表单
3. 选择竞品、填写日期和数据
4. 提交保存

---

## 📂 重要目录

```
/Users/xiaoxiao.liu/竞品监测平台/
├── data/competitors.db          # 数据库文件
├── data/reports/                # 生成的报告
├── data/backups/                # 数据库备份
├── logs/app.log                 # 应用日志
└── /Users/xiaoxiao.liu/竞品监测周报/  # 周报保存目录（兼容现有）
```

---

## 🛠️ 常用操作

### 查看日志
```bash
tail -f /Users/xiaoxiao.liu/竞品监测平台/logs/app.log
```

### 停止服务
```bash
# 前台运行：按 Ctrl+C

# 后台运行：
pkill -f app.py
```

### 重启服务
```bash
pkill -f app.py && ./start.sh
```

### 手动触发数据采集
1. 访问首页
2. 点击"立即采集数据"按钮
3. 或调用API：`curl -X POST http://localhost:5000/api/collection/trigger`

### 手动生成周报
1. 访问"历史报告"页面
2. 点击"生成新周报"按钮
3. 或调用API：`curl -X POST http://localhost:5000/api/reports/generate`

---

## 📊 与现有周报系统的兼容

✅ **完全兼容你现有的竞品监测周报系统**

- 生成的周报格式与skill文件定义完全一致
- 自动保存到 `/Users/xiaoxiao.liu/竞品监测周报/`
- 自动更新记忆文件 `weekly_competitor_report.md`
- 期数从第12期开始自动递增（接续你的第11期）

---

## 🎯 下一步建议

### 立即可做：
1. ✅ **启动平台**：运行 `./start.sh`
2. ✅ **访问Dashboard**：浏览器打开 http://localhost:5000
3. ✅ **配置API Key**：设置 ANTHROPIC_API_KEY 环境变量
4. ✅ **手动触发采集**：测试数据采集功能

### 短期优化：
1. 📊 **补充市场数据**：手动录入下载量、排名、评分
2. 🔄 **调整采集时间**：根据需要修改 `config.py`
3. 📧 **配置通知**：添加邮件或钉钉通知（待开发）

### 长期扩展：
1. 🤖 **AI分析**：集成Claude生成智能洞察
2. 📈 **数据可视化**：增强趋势图表
3. 📱 **移动端**：响应式设计已完成，可进一步优化

---

## ❓ 常见问题

### Q: 首次启动需要做什么？
A: 已完成初始化！直接运行 `./start.sh` 即可启动

### Q: 数据采集需要付费吗？
A: 使用Claude API需要付费（按使用量计费），每天约40次搜索请求

### Q: 不配置API Key可以用吗？
A: 可以！平台会正常运行，但自动采集功能不可用，需手动录入数据

### Q: 如何备份数据？
A: 数据库每周日自动备份，备份文件在 `data/backups/`

### Q: 可以在服务器上运行吗？
A: 可以！将项目复制到服务器，按相同方式启动即可

### Q: 如何卸载？
A: 直接删除 `/Users/xiaoxiao.liu/竞品监测平台/` 目录即可

---

## 📞 技术支持

- **日志文件**：`logs/app.log`
- **数据库位置**：`data/competitors.db`
- **配置文件**：`config.py`

---

## 🎉 完成！

你的竞品监测平台已经搭建完成并可以使用了！

**现在就启动吧：**

```bash
cd /Users/xiaoxiao.liu/竞品监测平台
./start.sh
```

然后在浏览器中访问：**http://localhost:5000**

---

**祝使用愉快！** 🚀
